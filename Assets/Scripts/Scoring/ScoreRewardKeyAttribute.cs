using UnityEngine;

namespace Massive.Scoring
{
    /// <summary>Select a reward identity; its value belongs to the active economy profile.</summary>
    public sealed class ScoreRewardKeyAttribute : PropertyAttribute { }
}
