namespace Massive.Enemies
{
    /// <summary>
    /// High-level classification used for spawn caps / UI / tuning.
    /// </summary>
    public enum EnemyCategory
    {
        Inert = 0,
        Ranged = 1,
        Melee = 2,
    }
}
